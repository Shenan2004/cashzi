# Copy this to .env and fill in the values

PORT=5001

# --- FOR LOCAL DEV (individual vars) ---
DB_USER=your_postgres_username
DB_PASSWORD=your_postgres_password
DB_HOST=localhost
DB_PORT=5432
DB_NAME=cashzi

# --- FOR PRODUCTION (Neon / cloud PostgreSQL) ---
# Paste your full Neon connection string here — this takes priority over individual vars above
# DATABASE_URL=postgresql://user:password@ep-xxx.neon.tech/neondb?sslmode=require

JWT_SECRET=replace_this_with_a_long_random_secret

# Optional: restrict CORS to an extra origin beyond the built-in allowlist
# CLIENT_URL=https://yourname.github.io
